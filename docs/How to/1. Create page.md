# Tạo page

## 1. Giới thiệu

Trong folder routes chứa các route trỏ tời các page, import page từ folder `pages` vào route để routing đến page đó.

## 2. Tạo page

_Routing_:
Để tạo một page với path là `http://localhost:1234/home` thì sẽ cần tạo file `📂pages/📂home/📜index.tsx`.

Config ở file routes/router.tsx rồi import route đó vào routes/index.tsx tương ứng với page cần routing tới

```
📦src
 ┣ 📂routes
 ┃ ┣ 📜index.tsx
 ┃ ┗ 📜router.tsx
```

```tsx
// Các biến có thể config được cho route
export type Routes = RouteObject & {
  path: string; // Đường link đến trang. Ví dụ đối với http://localhost:1234/home thì path sẽ là /home
  name?: string; // Tên của trang được hiển thị trên Menu và Breadcrumb (lưu ý: không bọc translation)
  screen?: ScreenType; // tên màn dùng để check quyền hiển thị 
  icon?: string | JSX.Element; // icon hiển thị trên menu
  hidden?: boolean; // nếu không muốn hiển thi lên Menu thì giá trị sẽ là true
  children?: Routes[] | undefined; // Khai báo thêm các route con thuộc route này
  element?: JSX.Element; // Page muốn routing tới
};

```

_Module_:  
Tương ứng với file home trên sẽ có một folder tương ứng trong module:

```
📂module
┣ 📂home
┃ ┣ 📂components
┃ ┃ ┣ 📂ModalCreateUpdate
┃ ┃ ┃ ┣ 📜validation.ts
┃ ┃ ┃ ┣ 📃index.scss
┃ ┃ ┃ ┣ 📜index.tsx
┃ ┣ 📃index.scss
┃ ┣ 📜index.tsx
```

Một page thường sẽ có cấu trúc như trên, gồm nhiều components tạo nên page. Những components này chỉ dùng cho page này nên chúng sẽ được đặt tại đây.
