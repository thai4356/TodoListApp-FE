export * from './role';

export enum TypeApi {
  GET_NEW,
  GET_MORE,
}

export enum TypeAction {
  EXPORT,
  DELETE,
}

export enum ETelComType {
  VIETTEL,
  MOBIFONE,
  VINAPHONE,
}

export enum Gender {
  MALE,
  FEMALE,
}

export enum EActionKey {
  Update,
  Delete,
  GetList,
  ConfigLicense,
  View,
  ChangePassword,
}
