import { createLogger } from 'redux-logger';

export default createLogger({
  // duration: import.meta.env.DEV,
  // timestamp: import.meta.env.DEV,
  // colors: {},
  // logErrors: import.meta.env.DEV,
  // diff: import.meta.env.DEV,
});
